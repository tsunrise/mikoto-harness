[package]
name = "apply-patch-core"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
similar = "2.7.0"
thiserror = "2.0.17"

[target.'cfg(unix)'.dependencies]
rustix = { version = "1.1.4", features = ["fs"] }

[dev-dependencies]
tempfile = "3.23.0"
